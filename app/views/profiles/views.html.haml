%h1 Profile views

%table.table.table-hover

  - @views.each do |view|
    %tr
      %td
        = link_to image_tag(view.viewer.image.thumb.url), show_user_profile_path(view.viewer)
      %td
        = view.viewer.full_name
        viewed your profile
      %td 
        = time_ago_in_words view.created_at
        %hr
