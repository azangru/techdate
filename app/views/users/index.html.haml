%h1 Users
.user-index
  %ul
    - @users.each do |u|
      %li
        = link_to image_tag(u.image.index.url), show_user_profile_path(u)
        %br
        #{link_to u.full_name, show_user_profile_path(u)} (#{u.profile.age} y.o.)